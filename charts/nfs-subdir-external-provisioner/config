export USE_OPENSOURCE_CHART=false

# must
export REPO_URL=https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner
export REPO_NAME=nfs-subdir-external-provisioner
export CHART_NAME=nfs-subdir-external-provisioner
export VERSION=4.0.17

# push to daocloud repo
export DAOCLOUD_REPO_PROJECT=addon
